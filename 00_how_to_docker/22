docker-machine ssh Aiur "$(docker swarm join-token worker | grep token)"

# or : docker-machine ssh Aiur "docker swarm join --token $(docker swarm join-token worker -q) $(docker-machine ip Char):2377"

# to leave swarm : docker-machine ssh Aiur "docker swarm leave"
# to remove node : docker swarm rm <ID>
